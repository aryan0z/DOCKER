in this task 1 we run a conatiner ubuntu
then we set a enviornment variable COLLGE=CSE
and then exited
so what we noticed is that if we don't set the enviornment variable in env file then it will be gone after exiting
